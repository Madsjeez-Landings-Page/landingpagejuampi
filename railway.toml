[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run start"
restartPolicyType = "on_failure"

# Nixpacks: Node 22 vía engines en package.json y .nvmrc (Next.js 16 necesita Node >= 20.9).

# Persistencia leads (opcional): Volume montado en /data + env LEADS_DATA_FILE=/data/leads.json

